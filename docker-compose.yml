services:
  app:
    build: .
    ports:
      - "3000:3000"
    # Banco persistido em /data (mesmo caminho do PVC no k3s).
    volumes:
      - db-data:/data
    env_file:
      - .env

volumes:
  db-data:
